import { Routes } from '@angular/router';
import { NumCapicuaComponent } from './taller/num-capicua/num-capicua.component';

export const routes: Routes = [
    {
        path: '',
        component: NumCapicuaComponent
    }
];
